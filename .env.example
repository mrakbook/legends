# Copy to .env and fill in as needed. The Makefile will auto-load this file if present.

# GitHub authentication (one of these is typically enough)
GITHUB_TOKEN=
GH_TOKEN=

# Default repo owner (user or org). Leave empty to use your authenticated default.
GHB_OWNER=

# CLI defaults
GHB_VISIBILITY=private         # private|public
GHB_BASE_BRANCH=main
GHB_REMOTE=origin
GHB_DRY_RUN=0                  # 1/true to print actions without executing

# Optional commit identity overrides (falls back to your GH account if empty)
GIT_AUTHOR_NAME=
GIT_AUTHOR_EMAIL=
# If you want committer to differ from author, set these; otherwise we default to author.*
GIT_COMMITTER_NAME=
GIT_COMMITTER_EMAIL=
